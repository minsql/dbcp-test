create table employee ( id int auto_increment primary key,
first_name varchar(10),
last_name varchar(10),
department varchar(30)
);

insert into employee (first_name, last_name, department) values('a','b','c');